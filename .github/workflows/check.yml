name: Check New Version

on:
  schedule:
    - cron: "0 8,20 * * *"
  workflow_dispatch:
  # Allow this workflow to be triggered manually or from other workflows

jobs:
  check-upstream:
    runs-on: ubuntu-latest
    outputs:
      needs_build: ${{ steps.check.outputs.needs_build }}
      new_digest: ${{ steps.check.outputs.new_digest }}
      old_digest: ${{ steps.check.outputs.old_digest }}
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Check upstream ghcr.io version
        id: check
        run: |
          set -euo pipefail

          # Fetch the image manifest to get the current digest
          # Using manifest list to get architecture-agnostic digest
          NEW_DIGEST=$(curl -sL "https://ghcr.io/v2/moltis-org/moltis/manifests/latest" \
            -H "Accept: application/vnd.docker.distribution.manifest.list.v2+json" \
            -H "Accept: application/vnd.docker.distribution.manifest.v2+json" \
            | jq -r '.digest' 2>/dev/null || echo "")

          if [ -z "$NEW_DIGEST" ]; then
            echo "Failed to fetch GHCR manifest — assuming network issue, skipping."
            echo "needs_build=false" >> $GITHUB_OUTPUT
            echo "new_digest=" >> $GITHUB_OUTPUT
            echo "old_digest=" >> $GITHUB_OUTPUT
            exit 0
          fi

          echo "New upstream digest: $NEW_DIGEST"

          # Retrieve previously cached digest from git commit message
          OLD_DIGEST=$(git log --all --oneline --grep="upstream_digest:" -1 2>/dev/null \
            | grep -oP "upstream_digest:\s*\K[a-f0-9:]+" || echo "")

          echo "Cached digest:      $OLD_DIGEST"
          echo "new_digest=$NEW_DIGEST" >> $GITHUB_OUTPUT
          echo "old_digest=$OLD_DIGEST" >> $GITHUB_OUTPUT

          if [ "$OLD_DIGEST" = "$NEW_DIGEST" ]; then
            echo "needs_build=false" >> $GITHUB_OUTPUT
            echo "No new version — upstream digest unchanged."
          elif [ -z "$OLD_DIGEST" ]; then
            echo "needs_build=false" >> $GITHUB_OUTPUT
            echo "No cached digest found — treating as first run, no build triggered."
          else
            echo "needs_build=true" >> $GITHUB_OUTPUT
            echo "New version detected: $OLD_DIGEST → $NEW_DIGEST"
          fi

  # Human-in-the-loop: create a dispatch issue when a new version is found,
  # so a maintainer can review and trigger the build manually.
  alert:
    needs: check-upstream
    if: needs.check-upstream.outputs.needs_build == 'true'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Create dispatch issue
        env:
          NEW_DIGEST: ${{ needs.check-upstream.outputs.new_digest }}
          OLD_DIGEST: ${{ needs.check-upstream.outputs.old_digest }}
        run: |
          BODY="## 🚀 New Moltis upstream version detected

A new version of **ghcr.io/moltis-org/moltis:latest** has been detected.

| | Digest |
|---|---|
| **Previous** | \`$OLD_DIGEST\` |
| **New** | \`$NEW_DIGEST\` |

---

Click **Actions** → **Build & Push Moltis Container Image** → **Run workflow** to rebuild and push the image to Docker Hub.

---

*This issue was created automatically by the Check New Version workflow.*"

          gh issue create \
            --title "🚢 Dispatch: Rebuild for upstream digest $NEW_DIGEST" \
            --body "$BODY" \
            --repo Frozen12/moltis-container \
            --label "dispatch,automated"
